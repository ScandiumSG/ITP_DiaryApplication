image: maven:latest

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build
  script:
    - mvn compile -B
    - mvn test -B

tests:
  stage: test
  script:
    - mvn test -B
  only: [merge_requests]